#include <stdio.h>
int main()
{
    int n, opcion;

    do
    {
        printf( "Bienvenido al buscaminas, ¿quiere jugar? \n" );
        printf( "si quiero [1], no quiero [2] \n" );

        scanf ("%d", &opcion);

        switch(opcion)
        {
            case 1:
                break;
        }
    } while (opcion!=2);

    return 0;
}
